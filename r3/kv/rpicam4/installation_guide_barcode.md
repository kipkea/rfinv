# คู่มือการติดตั้งและใช้งาน: RPi Camera Controller (Barcode/QR Version)

**สำหรับ Raspberry Pi 5 (OS: Bookworm)**

โปรแกรมนี้พัฒนาด้วย **Python** และ **Kivy** เพื่อสร้าง UI สำหรับจอสัมผัส และใช้ **OpenCV** ร่วมกับ **Pyzbar** สำหรับการควบคุมกล้องและการอ่าน Barcode/QRCode แบบ Real-time

## 1. ข้อกำหนดเบื้องต้น (Prerequisites)

| รายการ | รายละเอียด |
| :--- | :--- |
| **ฮาร์ดแวร์** | Raspberry Pi 5, กล้อง (CSI หรือ USB), จอสัมผัส (Touchscreen) |
| **ระบบปฏิบัติการ** | Raspberry Pi OS Bookworm (64-bit) |
| **ซอฟต์แวร์** | Python 3.9+ |

## 2. การติดตั้ง Dependencies

เปิด Terminal บน Raspberry Pi ของคุณและรันคำสั่งต่อไปนี้:

### 2.1 ติดตั้ง Kivy และ Dependencies พื้นฐาน

```bash
# อัปเดตระบบ
sudo apt update && sudo apt upgrade -y

# ติดตั้ง Dependencies สำหรับ Kivy (SDL2)
sudo apt install -y libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev pkg-config

# ติดตั้ง Kivy
pip3 install kivy
```

### 2.2 ติดตั้ง OpenCV และ Pyzbar

เนื่องจากโปรแกรมนี้ใช้ OpenCV และ Pyzbar สำหรับการประมวลผลภาพและการอ่านรหัส จึงจำเป็นต้องติดตั้งไลบรารีเหล่านี้:

```bash
# ติดตั้ง OpenCV (เวอร์ชันที่เหมาะสมกับ RPi)
# หมายเหตุ: การติดตั้ง OpenCV อาจใช้เวลานาน
pip3 install opencv-python

# ติดตั้ง Pyzbar สำหรับการอ่าน Barcode/QRCode
pip3 install pyzbar
```

## 3. การเตรียมโปรเจกต์

1.  **แตกไฟล์โปรเจกต์:** แตกไฟล์ `rpi_camera_controller_opencv_barcode.zip` ไปยังโฟลเดอร์ที่คุณต้องการ (เช่น `/home/pi/rpi_camera_controller`)
2.  **สร้างโฟลเดอร์จัดเก็บ:** โปรแกรมจะบันทึกไฟล์ภาพ/วิดีโอในโฟลเดอร์ `images/` ซึ่งจะถูกสร้างขึ้นอัตโนมัติในระดับเดียวกับไฟล์โปรเจกต์

## 4. การใช้งานโปรแกรม

1.  **เข้าสู่โฟลเดอร์โปรเจกต์:**
    ```bash
    cd /home/pi/rpi_camera_controller
    ```

2.  **รันโปรแกรม:**
    ```bash
    python3 main.py
    ```

### 4.1 ฟีเจอร์ Barcode/QRCode Detection

*   **ปุ่ม:** กดปุ่ม **"🔍 Barcode: OFF"** เพื่อเปิดใช้งานฟีเจอร์ (ปุ่มจะเปลี่ยนเป็น **"🔍 Barcode: ON"**)
*   **การทำงาน:** เมื่อเปิดใช้งาน โปรแกรมจะพยายามตรวจจับ Barcode (เช่น Code 128, EAN) และ QRCode ในภาพพรีวิวแบบ Real-time
*   **ผลลัพธ์:**
    *   หากตรวจพบ: จะแสดงกรอบสีเขียวรอบรหัส และแสดงผลลัพธ์ (ประเภทและข้อมูล) ที่แถบสถานะด้านล่าง
    *   หากไม่พบ: จะแสดง **"❌ ไม่พบ Barcode/QR"** ที่แถบสถานะ

### 4.2 ฟีเจอร์ Image Processing

*   **ปุ่ม:** กดปุ่ม **"🖼️ ปรับภาพ"** เพื่อเปิด Popup สำหรับปรับแต่งภาพ
*   **การปรับแต่ง:** คุณสามารถปรับ **ความสว่าง**, **ความคมชัด**, **ความคม**, **ขนาดภาพ**, **โหมดสี** และ **ฟิลเตอร์** (เช่น Sobel, Canny) ได้แบบ Real-time

### 4.3 การบันทึกไฟล์

*   **ถ่ายภาพ:** กดปุ่ม **"📸 ถ่ายภาพ"** จะเปิด Popup ให้คุณตั้งชื่อไฟล์และใส่ Tag (Tag จะถูกรวมในชื่อไฟล์)
*   **บันทึกวิดีโอ:** กดปุ่ม **"🔴 บันทึก"** เพื่อเริ่มบันทึก และกดปุ่ม **"⏹️ หยุดบันทึก"** เพื่อหยุด (วิดีโอจะถูกบันทึกในโฟลเดอร์ `images/`)

## 5. การแก้ไขปัญหาเบื้องต้น

*   **กล้องไม่ทำงาน:** ตรวจสอบว่ากล้องของคุณถูกเปิดใช้งานใน `raspi-config` และสามารถเข้าถึงได้ผ่าน `/dev/video0` (สำหรับ USB Camera) หรือตรวจสอบการตั้งค่า `libcamera` (สำหรับ CSI Camera)
*   **Kivy UI ไม่แสดงผล:** ตรวจสอบว่าคุณได้ติดตั้ง Dependencies ของ SDL2 ครบถ้วนตามขั้นตอนที่ 2.1 แล้ว
*   **ข้อผิดพลาด `ImportError: No module named 'pyzbar'`:** ตรวจสอบว่าคุณได้รัน `pip3 install pyzbar` แล้ว
