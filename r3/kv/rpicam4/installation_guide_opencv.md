# คู่มือการติดตั้งและใช้งาน: RPi Camera Controller (OpenCV & Kivy)

## 1. ภาพรวมโปรเจกต์

โปรเจกต์นี้คือโปรแกรมควบคุมกล้องสำหรับ Raspberry Pi 5 ที่ใช้ระบบปฏิบัติการ **Raspberry Pi OS Bookworm** โดยมีคุณสมบัติดังนี้:

*   **กล้อง:** ใช้ **OpenCV** (cv2) ในการเข้าถึงและแสดงผลพรีวิวกล้อง
*   **UI:** ใช้ **Kivy** Framework ในการสร้าง Single-Screen UI ที่มีปุ่มขนาดใหญ่ เหมาะสำหรับจอสัมผัส
*   **จัดเก็บข้อมูล:** อัปโหลดภาพและวิดีโอไปยัง **AWS S3**
*   **ฟีเจอร์:** ตั้งชื่อไฟล์อัตโนมัติ/กำหนดเอง, ใส่ Tag, ตั้งค่าเครือข่าย (จำลอง), และแชร์ไป Facebook

## 2. ข้อกำหนดเบื้องต้น (Prerequisites)

*   **ฮาร์ดแวร์:** Raspberry Pi 5 (แนะนำ RAM 4GB ขึ้นไป) พร้อมกล้อง CSI หรือ USB Camera
*   **ซอฟต์แวร์:** Raspberry Pi OS Bookworm (64-bit)
*   **บัญชี:** บัญชี AWS (สำหรับ S3) และบัญชี Facebook Developer (สำหรับ Access Token)

## 3. ขั้นตอนการติดตั้ง (Installation Steps)

### 3.1. ติดตั้ง Dependencies ของระบบ

เปิด Terminal บน Raspberry Pi และรันคำสั่งต่อไปนี้:

```bash
# อัปเดตระบบ
sudo apt update && sudo apt upgrade -y

# ติดตั้ง Dependencies สำหรับ Kivy และ OpenCV
sudo apt install -y python3-pip libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev pkg-config
sudo apt install -y libopencv-dev python3-opencv # ติดตั้ง OpenCV
```

### 3.2. ติดตั้ง Python Libraries

สร้าง Virtual Environment (แนะนำ) และติดตั้ง Python Libraries ที่จำเป็น:

```bash
# สร้างและเปิดใช้งาน Virtual Environment
python3 -m venv venv
source venv/bin/activate

# ติดตั้ง Kivy, Boto3 (AWS SDK), และ Requests (สำหรับ Facebook API)
pip install kivy boto3 requests
```

### 3.3. การตั้งค่ากล้อง (OpenCV on Bookworm)

เนื่องจาก Bookworm ใช้ `libcamera` แทน `V4L2` แบบเดิม การใช้ `cv2.VideoCapture(0)` อาจมีปัญหาในการเข้าถึงกล้อง CSI (กล้องที่ต่อผ่านสายแพร)

**ทางเลือกที่ 1: ใช้ USB Camera**
*   เสียบ USB Camera และรันโปรแกรมได้เลย (มักจะทำงานได้ดีกว่า)

**ทางเลือกที่ 2: ใช้ CSI Camera (อาจต้องตั้งค่าเพิ่มเติม)**
*   ตรวจสอบว่ากล้องถูกเปิดใช้งานใน `raspi-config`
*   หากยังใช้งานไม่ได้ อาจต้องติดตั้ง GStreamer Backend หรือใช้ **Hybrid Approach** (ใช้ Picamera2 ดึงเฟรมแล้วแปลงเป็น OpenCV Frame) ซึ่งอยู่นอกเหนือขอบเขตของโค้ดตัวอย่างนี้ แต่เป็นแนวทางที่แนะนำสำหรับ CSI Camera บน Bookworm

### 3.4. การตั้งค่าโปรเจกต์

1.  **แตกไฟล์โปรเจกต์:** แตกไฟล์ `rpi_camera_controller_opencv.zip` ที่คุณได้รับ
2.  **ตั้งค่า AWS และ Facebook:** แก้ไขไฟล์ **`rpi_camera_controller_opencv/config.py`** และแทนที่ค่า Placeholder ด้วยข้อมูลจริงของคุณ:

    ```python
    AWS_ACCESS_KEY_ID = "YOUR_AWS_ACCESS_KEY_ID"
    AWS_SECRET_ACCESS_KEY = "YOUR_AWS_SECRET_ACCESS_KEY"
    S3_BUCKET_NAME = "your-rpi-camera-bucket-name"
    FACEBOOK_ACCESS_TOKEN = "YOUR_FACEBOOK_ACCESS_TOKEN"
    ```

## 4. การใช้งานโปรแกรม

### 4.1. การรันโปรแกรม

ตรวจสอบให้แน่ใจว่าคุณอยู่ใน Virtual Environment และอยู่ในไดเรกทอรีหลักของโปรเจกต์:

```bash
# ตรวจสอบว่าอยู่ใน venv
(venv) $ 

# รันโปรแกรม
python3 rpi_camera_controller_opencv/main.py
```

### 4.2. การใช้งาน UI (Single-Screen)

*   **Camera Preview (70%):** แสดงผลพรีวิวกล้องแบบสด
*   **ปุ่มควบคุม (20%):**
    *   **📸 ถ่ายภาพ:** กดเพื่อถ่ายภาพนิ่ง จะมี Popup ให้ใส่ชื่อไฟล์และ Tag ก่อนอัปโหลด
    *   **🔴 บันทึก/⏹️ หยุดบันทึก:** กดเพื่อเริ่ม/หยุดบันทึกวิดีโอ เมื่อหยุดแล้วจะมี Popup ให้ใส่ชื่อไฟล์และ Tag ก่อนอัปโหลด
    *   **⚙️ ตั้งค่า:** เปิด Popup สำหรับใส่ AWS Credentials และตั้งค่า Wi-Fi (จำลอง)
    *   **📂 ประวัติ:** เปิด Popup เพื่อดูรายการไฟล์ที่อัปโหลดล่าสุดจาก S3
    *   **📤 แชร์:** แชร์ไฟล์ที่อัปโหลดล่าสุดไปยัง Facebook

*   **Status Bar (10%):** แสดงสถานะการทำงานปัจจุบัน (เช่น "Ready", "Uploading...", "Error")

## 5. หมายเหตุสำคัญ

*   **ความปลอดภัย AWS:** การเก็บ Access Key ในไฟล์ `config.py` ไม่ปลอดภัยอย่างยิ่ง ควรใช้ **IAM Roles** บน Raspberry Pi หรือ **AWS Secrets Manager** ในสภาพแวดล้อมจริง
*   **การตั้งค่าเครือข่าย:** ฟังก์ชันการตั้งค่าเครือข่ายใน `network_service.py` เป็นการจำลองและต้องใช้สิทธิ์ `sudo` ในการรันคำสั่ง `nmcli` บน Raspberry Pi จริง
*   **วิดีโอ:** การบันทึกวิดีโอด้วย OpenCV อาจต้องมีการแปลงไฟล์ (เช่น จาก AVI เป็น MP4) ก่อนอัปโหลดไปยัง S3 เพื่อให้สามารถเล่นบนเว็บเบราว์เซอร์หรือแชร์ไป Facebook ได้อย่างราบรื่น

***

### References

[1] OpenCV VideoCapture on Raspberry Pi OS Bookworm. *OpenCV Forum*.
[2] Kivy Installation on Raspberry Pi. *Kivy Documentation*.
[3] AWS S3 Boto3 Documentation. *AWS Documentation*.
