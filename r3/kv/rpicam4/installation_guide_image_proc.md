# คู่มือการติดตั้งและใช้งาน: RPi Camera Controller (OpenCV & Local Storage with Image Processing)

## 1. ภาพรวมโปรเจกต์

โปรเจกต์นี้คือโปรแกรมควบคุมกล้องสำหรับ Raspberry Pi 5 ที่ใช้ระบบปฏิบัติการ **Raspberry Pi OS Bookworm** โดยมีคุณสมบัติเพิ่มเติมคือ **Image Processing Pipeline** แบบ Real-time:

*   **กล้อง:** ใช้ **OpenCV** (cv2) ในการเข้าถึงและแสดงผลพรีวิวกล้อง
*   **UI:** ใช้ **Kivy** Framework ในการสร้าง Single-Screen UI ที่มีปุ่มขนาดใหญ่ เหมาะสำหรับจอสัมผัส
*   **Image Processing:** สามารถปรับ **ความสว่าง (Brightness)**, **ความคมชัด (Contrast)**, **ความคม (Sharpness)**, **โหมดสี (Color Mode)**, และใช้ **ฟิลเตอร์ (Sobel, Canny)** ได้แบบ Real-time
*   **จัดเก็บข้อมูล:** บันทึกภาพและวิดีโอในโฟลเดอร์ **`images/`** ภายในเครื่อง

## 2. ข้อกำหนดเบื้องต้น (Prerequisites)

*   **ฮาร์ดแวร์:** Raspberry Pi 5 (แนะนำ RAM 4GB ขึ้นไป) พร้อมกล้อง CSI หรือ USB Camera
*   **ซอฟต์แวร์:** Raspberry Pi OS Bookworm (64-bit)

## 3. ขั้นตอนการติดตั้ง (Installation Steps)

### 3.1. ติดตั้ง Dependencies ของระบบ

เปิด Terminal บน Raspberry Pi และรันคำสั่งต่อไปนี้:

```bash
# อัปเดตระบบ
sudo apt update && sudo apt upgrade -y

# ติดตั้ง Dependencies สำหรับ Kivy และ OpenCV
sudo apt install -y python3-pip libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev pkg-config
sudo apt install -y libopencv-dev python3-opencv # ติดตั้ง OpenCV
```

### 3.2. ติดตั้ง Python Libraries

สร้าง Virtual Environment (แนะนำ) และติดตั้ง Python Libraries ที่จำเป็น:

```bash
# สร้างและเปิดใช้งาน Virtual Environment
python3 -m venv venv
source venv/bin/activate

# ติดตั้ง Kivy
pip install kivy
```

### 3.3. การตั้งค่าโปรเจกต์

1.  **แตกไฟล์โปรเจกต์:** แตกไฟล์ `rpi_camera_controller_opencv_local_fixed_v7.zip` ที่คุณได้รับ
2.  **สร้างโฟลเดอร์เก็บไฟล์:** โปรแกรมจะสร้างโฟลเดอร์ `images/` ให้โดยอัตโนมัติ
    ```bash
    mkdir rpi_camera_controller_opencv/images
    ```

## 4. การใช้งานโปรแกรม

### 4.1. การรันโปรแกรม

ตรวจสอบให้แน่ใจว่าคุณอยู่ใน Virtual Environment และอยู่ในไดเรกทอรีหลักของโปรเจกต์:

```bash
# ตรวจสอบว่าอยู่ใน venv
(venv) $ 

# รันโปรแกรม
python3 rpi_camera_controller_opencv/main.py
```

### 4.2. การใช้งาน UI (Single-Screen)

*   **Camera Preview (70%):** แสดงผลพรีวิวกล้องแบบสด **พร้อมการปรับแต่งภาพแบบ Real-time**
*   **ปุ่มควบคุม (20%):**
    *   **📸 ถ่ายภาพ:** กดเพื่อถ่ายภาพนิ่ง จะมี Popup ให้ใส่ชื่อไฟล์และ Tag ก่อนบันทึก
    *   **🔴 บันทึก/⏹️ หยุดบันทึก:** กดเพื่อเริ่ม/หยุดบันทึกวิดีโอ เมื่อหยุดแล้วจะมี Popup ให้ใส่ชื่อไฟล์และ Tag ก่อนบันทึก
    *   **🖼️ ปรับภาพ:** เปิด Popup สำหรับปรับแต่งภาพ Real-time
    *   **⚙️ ตั้งค่า:** เปิด Popup สำหรับตั้งค่า Wi-Fi (จำลอง)

### 4.3. การปรับแต่งภาพ Real-time

เมื่อกดปุ่ม **🖼️ ปรับภาพ** จะมี Popup แสดงขึ้นมาพร้อม Slider และ Dropdown สำหรับปรับค่าต่างๆ:

| การตั้งค่า | คำอธิบาย | ช่วงค่า |
| :--- | :--- | :--- |
| **ความสว่าง (Brightness)** | ปรับความสว่างของภาพ | 0.0 (มืดสุด) ถึง 2.0 (สว่างสุด) |
| **ความคมชัด (Contrast)** | ปรับความคมชัดของภาพ | 0.0 (จางสุด) ถึง 2.0 (เข้มสุด) |
| **ความคม (Sharpness)** | ปรับความคมของภาพ (จำลอง) | 0 (ปกติ) ถึง 2 (คมชัดมาก) |
| **ขนาดภาพ (Resize Factor)** | ปรับขนาดภาพที่แสดงผล | 0.5 (เล็กสุด) ถึง 2.0 (ใหญ่สุด) |
| **โหมดสี (Color Mode)** | เปลี่ยนโหมดสี (BGR, GRAY, HSV) | BGR, GRAY, HSV |
| **ฟิลเตอร์ (Filter)** | ใช้ฟิลเตอร์ตรวจจับขอบ | None, Sobel, Canny |

**หมายเหตุ:** ภาพและวิดีโอที่บันทึกจะใช้ **Processed Frame** ที่คุณเห็นบนหน้าจอ

## 5. หมายเหตุสำคัญ

*   **ประสิทธิภาพ:** การประมวลผลภาพแบบ Real-time อาจใช้ทรัพยากร CPU สูงบน Raspberry Pi 5 หากพบปัญหาเฟรมเรตตก ให้ลองลดค่า `resize_factor` หรือปิดฟิลเตอร์ที่ซับซ้อน
*   **การตั้งค่าเครือข่าย:** ฟังก์ชันการตั้งค่าเครือข่ายใน `network_service.py` เป็นการจำลองและต้องใช้สิทธิ์ `sudo` ในการรันคำสั่ง `nmcli` บน Raspberry Pi จริง

***

### References

[1] OpenCV VideoCapture on Raspberry Pi OS Bookworm. *OpenCV Forum*.
[2] Kivy Installation on Raspberry Pi. *Kivy Documentation*.
[3] OpenCV Image Processing: Brightness and Contrast. *OpenCV Documentation*.
[4] OpenCV Edge Detection: Sobel and Canny. *OpenCV Documentation*.
